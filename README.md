# Derfcorder — Releases

Public installers + auto-update feed for **Derfcorder** (a tape-recorder desktop app). The application source is private; this repo holds only built release artifacts that the app's auto-updater reads.

Download the latest `Derfcorder Setup x.y.z.exe` from the [Releases](../../releases) page.
